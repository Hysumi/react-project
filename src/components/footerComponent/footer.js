import React, { Component } from "react";

// components

class Footer extends Component {
    render () {
        return (
            <footer>

            </footer>
        );
    }
}

export default Footer;
