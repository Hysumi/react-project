import React, { Component } from "react";

// components

class Header extends Component {
    render () {
        return (
            <header>
                Test Header
            </header>
        );
    }
}

export default Header;
